
import React from 'react';
// Props is used for assigning and Reuse of Component With Custom Value

function Props(props){
    return(
        <h1>Hello, I am {props.name} and my age is {props.age}</h1>
    );
}

export default Props